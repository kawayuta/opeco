<%= form_for :shares,url: {action: :create} do |form| %>
    <div class="field">
      <%= form.text_field :username %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
